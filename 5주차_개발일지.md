# 5주차 개발일지

---

## 04/16

**오늘 공부 / 개발 한 것**
- 배포 수준 점검 — 보안, 에러 처리, 성능, UI/UX 전반 분석 및 우선순위 정리
- CORS 와일드카드(`*`) 제거, 허용 도메인 명시적 설정으로 보안 강화
- 이메일 형식 정규식 검증 추가
- 비밀번호 정책 강화 — 영문 + 숫자 혼합 8자 이상 검증
- `.env.example` 파일 생성 — 환경변수 문서화

**오류 / 어려웠던 점**
- CORS 설정에서 `CLIENT_URL`을 콤마로 여러 개 받을 수 있도록 처리하는 방법 고민
- 기존 회원 비밀번호가 새 정책에 안 맞을 수 있어서 회원가입에만 적용하고 로그인은 그대로 유지

**내일 할 일**
- Q&A N+1 쿼리 최적화
- DB 인덱스 추가
- 결제 시스템 설계 시작

---

## 04/17

**오늘 공부 / 개발 한 것**
- Q&A 목록 조회 N+1 쿼리 문제 해결 — 각 질문마다 답변 쿼리를 따로 날리던 것을 JOIN 단일 쿼리로 최적화
- DB 인덱스 추가 SQL 파일 생성 (`migrate_add_indexes.sql`)
  - courses, enrollments, progress, reviews, wishlists, qna 등 자주 조회되는 컬럼에 인덱스 추가
- wishlist, instructorController 쿼리에서 제거된 `lecture_count`, `total_hours` 컬럼 참조 수정
- 강사 대시보드에 Q&A 탭 추가 — 커리큘럼 / Q&A 탭 전환, 질문 목록 확인 및 답변 등록
- 강사 답변에 "강사" 뱃지 표시, Enter 키로 답변 등록 가능

**오류 / 어려웠던 점**
- N+1 최적화 후 JOIN 결과를 메모리에서 그룹핑하는 로직 작성이 복잡했음
- wishlist 쿼리에서 `c.lecture_count` 컬럼이 없어서 500 에러 발생 → 실시간 COUNT로 교체

**내일 할 일**
- Toss Payments 결제 시스템 구현
- 결제 성공/실패 페이지 구현

---

## 04/20

**오늘 공부 / 개발 한 것**
- Toss Payments 결제 시스템 구현 (결제 위젯 방식)
  - `payment_orders` 테이블 추가 (schema.sql)
  - 결제 준비 API (`POST /api/payment/prepare/:courseId`)
  - 결제 승인 API (`POST /api/payment/confirm`) — 금액 위변조 검증, 트랜잭션 처리
  - 결제 취소/환불 API (`POST /api/payment/cancel/:courseId`)
- `@tosspayments/tosspayments-sdk` 패키지 설치
- PaymentPage 구현 — 강의 정보, 결제 위젯 렌더링, 결제하기 버튼
- PaymentResult 구현 — 결제 성공/실패 결과 페이지
- CourseDetail 수강 신청 버튼 → 결제 페이지로 이동하도록 변경

**오류 / 어려웠던 점**
- API 개별 연동 키(ck/sk)로 했더니 테스트 카드 자동입력 버튼이 없음 → 결제 위젯 방식(gck/gsk)으로 전환
- 사업자등록번호 없어도 Toss 개발자 계정으로 테스트 키 발급 가능한 것 확인
- `vite-env.d.ts` 파일 없어서 `import.meta.env` 타입 에러 발생 → 파일 추가로 해결

**내일 할 일**
- 결제 리다이렉트 URL 처리 문제 해결
- 강사 프로필 수정 페이지 구현

---

## 04/21

**오늘 공부 / 개발 한 것**
- 결제 완료 후 Toss 리다이렉트 처리 문제 해결
  - App.tsx에 URL 파라미터 파싱 로직 추가 (`getInitialState`)
  - `paymentKey` 존재 여부로 성공/실패 판단하도록 수정
  - 처리 완료 후 `window.history.replaceState`로 URL 초기화 (새로고침 방지)
- 강사 프로필 수정 페이지(`InstructorProfile`) 신규 생성
  - 이름, 소개글, 프로필 사진 변경 가능
  - 저장 시 instructors + users 테이블 동시 업데이트
- 강사 닉네임/뱃지 클릭 시 프로필 페이지로 이동
- Nav에서 강사 역할일 때 "강의" 메뉴 제거, 역할별 메뉴 완전 분리

**오류 / 어려웠던 점**
- 결제 성공 후 새로고침하면 결제 실패창이 계속 뜨는 문제 → URL 초기화로 해결
- 결제 성공/실패 창이 동시에 뜨는 문제 → URL의 `paymentKey` 파라미터 존재 여부로 판단하도록 수정
- Toss successUrl과 failUrl 모두 `page=` 파라미터를 쓰다 보니 충돌 발생

**내일 할 일**
- 진도율 오르는 기준 점검
- 전체 기능 최종 테스트
- 커밋 및 푸시

---

## 04/22

**오늘 공부 / 개발 한 것**
- 진도율 계산 쿼리 수정 — `c.lecture_count` 고정 컬럼 대신 lectures 테이블에서 실시간 COUNT
- 내 수업 페이지 썸네일 실제 데이터 연동 (`e.thumbnail_url` 사용)
- 강의 삭제 시 FK 제약 오류 수정 — enrollments, progress, reviews, wishlists, qna 순서대로 삭제 후 courses 삭제
- 수강생 있는 강의 삭제 시 경고 메시지 표시
- 전체 기능 최종 테스트 및 오류 수정
- Git 커밋 및 푸시

**오류 / 어려웠던 점**
- 강의 삭제 시 `ER_ROW_IS_REFERENCED_2` FK 제약 오류 → 관련 테이블 데이터를 순서대로 삭제하는 로직 추가
- 진도율이 0%로 고정되는 문제 → lectures 테이블에 데이터가 없으면 lecture_count가 0이라 계산 불가, 강의 영상 추가 필요
- git push 시 remote에 로컬에 없는 커밋 존재 → `git pull --rebase` 후 push

**내일 할 일**
- 추가 기능 개선 사항 검토
- 문서 정리
