# ERD — SOMA 요가 인강 플랫폼

## 1. ER 다이어그램 (Mermaid)

```mermaid
erDiagram
    users {
        INT id PK "자동 증가"
        VARCHAR name "이름 (NOT NULL)"
        VARCHAR nickname "닉네임 (NOT NULL)"
        VARCHAR email "이메일 (NOT NULL, UNIQUE)"
        VARCHAR password_hash "비밀번호 해시 (NOT NULL)"
        ENUM role "user / instructor / admin (DEFAULT user)"
        DATETIME created_at "등록일"
        DATETIME updated_at "최종 수정일"
    }

    instructors {
        INT id PK "자동 증가"
        VARCHAR name "강사명 (NOT NULL)"
        TEXT bio "소개글"
        VARCHAR avatar_url "프로필 이미지 URL"
        JSON certifications "자격증 목록"
        DATETIME created_at "등록일"
    }

    categories {
        INT id PK "자동 증가"
        VARCHAR name "카테고리명 (UNIQUE) — 입문/중급/심화"
    }

    courses {
        INT id PK "자동 증가"
        INT instructor_id FK "강사 참조"
        INT category_id FK "카테고리 참조"
        VARCHAR title "강의 제목 (NOT NULL)"
        TEXT description "강의 설명"
        ENUM level "입문 / 중급 / 심화"
        INT duration_weeks "기간(주) nullable"
        VARCHAR thumbnail_url "썸네일 URL"
        INT price "수강료 (원)"
        TINYINT is_live "라이브 여부"
        TINYINT is_published "공개 여부"
        DATETIME created_at "등록일"
        DATETIME updated_at "최종 수정일"
    }

    lectures {
        INT id PK "자동 증가"
        INT course_id FK "강의 참조"
        TINYINT week "주차"
        TINYINT sort_order "정렬 순서"
        VARCHAR title "강의 제목 (NOT NULL)"
        INT duration_sec "재생 시간(초)"
        TINYINT is_preview "무료 미리보기 여부"
        VARCHAR video_url "유튜브 영상 ID"
    }

    enrollments {
        INT id PK "자동 증가"
        INT user_id FK "수강생 참조"
        INT course_id FK "강의 참조"
        INT paid_price "결제 금액"
        DATETIME enrolled_at "수강 신청일"
    }

    progress {
        INT id PK "자동 증가"
        INT user_id FK "수강생 참조"
        INT lecture_id FK "강의 참조"
        INT watched_sec "시청 위치(초)"
        TINYINT is_completed "완료 여부"
        DATETIME last_watched "마지막 시청일"
    }

    reviews {
        INT id PK "자동 증가"
        INT user_id FK "작성자 참조"
        INT course_id FK "강의 참조"
        TINYINT rating "별점 1~5"
        TEXT content "후기 내용"
        DATETIME created_at "작성일"
    }

    wishlists {
        INT user_id FK "수강생 참조"
        INT course_id FK "강의 참조"
        DATETIME created_at "찜한 날짜"
    }

    qna_questions {
        INT id PK "자동 증가"
        INT user_id FK "질문자 참조"
        INT course_id FK "강의 참조"
        TEXT content "질문 내용"
        DATETIME created_at "등록일"
    }

    qna_answers {
        INT id PK "자동 증가"
        INT question_id FK "질문 참조"
        INT user_id FK "답변자 참조"
        TEXT content "답변 내용"
        DATETIME created_at "등록일"
    }

    users ||--o{ enrollments : "수강 신청"
    users ||--o{ progress : "진도 기록"
    users ||--o{ reviews : "후기 작성"
    users ||--o{ wishlists : "찜하기"
    users ||--o{ qna_questions : "질문 등록"
    users ||--o{ qna_answers : "답변 등록"
    instructors ||--o{ courses : "강의 개설"
    categories ||--o{ courses : "카테고리 분류"
    courses ||--o{ lectures : "커리큘럼"
    courses ||--o{ enrollments : "수강 신청 대상"
    courses ||--o{ reviews : "후기 대상"
    courses ||--o{ wishlists : "찜 대상"
    courses ||--o{ qna_questions : "Q&A 대상"
    lectures ||--o{ progress : "진도 기록 대상"
    qna_questions ||--o{ qna_answers : "답변"
```

## 2. 테이블 상세

### 2.1 users

| 컬럼 | 타입 | 제약조건 | 설명 |
|------|------|---------|------|
| id | INT UNSIGNED | PK, AUTO_INCREMENT | 고유 식별자 |
| name | VARCHAR(50) | NOT NULL | 실명 |
| nickname | VARCHAR(50) | NOT NULL | 닉네임 |
| email | VARCHAR(100) | NOT NULL, UNIQUE | 이메일 (로그인 ID) |
| password_hash | VARCHAR(255) | NOT NULL | bcrypt 해시 |
| role | ENUM | NOT NULL, DEFAULT 'user' | user / instructor / admin |
| created_at | DATETIME | NOT NULL | 가입일 |
| updated_at | DATETIME | NOT NULL | 최종 수정일 |

### 2.2 courses

| 컬럼 | 타입 | 제약조건 | 설명 |
|------|------|---------|------|
| id | INT UNSIGNED | PK, AUTO_INCREMENT | 고유 식별자 |
| instructor_id | INT UNSIGNED | FK → instructors(id) | 강사 |
| category_id | INT UNSIGNED | FK → categories(id) | 카테고리 |
| title | VARCHAR(200) | NOT NULL | 강의 제목 |
| level | ENUM | NOT NULL | 입문 / 중급 / 심화 |
| price | INT UNSIGNED | NOT NULL | 수강료 (원) |
| is_live | TINYINT(1) | NOT NULL, DEFAULT 0 | 라이브 여부 |
| is_published | TINYINT(1) | NOT NULL, DEFAULT 0 | 공개 여부 |

> **주의:** `lecture_count`, `total_hours`는 courses 테이블에 저장하지 않는다. lectures 테이블에서 `COUNT`, `SUM`으로 실시간 계산한다.

### 2.3 lectures

| 컬럼 | 타입 | 제약조건 | 설명 |
|------|------|---------|------|
| id | INT UNSIGNED | PK, AUTO_INCREMENT | 고유 식별자 |
| course_id | INT UNSIGNED | FK → courses(id), CASCADE | 소속 강의 |
| week | TINYINT UNSIGNED | NOT NULL, DEFAULT 1 | 주차 |
| sort_order | TINYINT UNSIGNED | NOT NULL, DEFAULT 1 | 주차 내 순서 |
| video_url | VARCHAR(255) | nullable | 유튜브 영상 ID |

### 2.4 progress

| 컬럼 | 타입 | 제약조건 | 설명 |
|------|------|---------|------|
| id | INT UNSIGNED | PK, AUTO_INCREMENT | 고유 식별자 |
| user_id | INT UNSIGNED | FK → users(id) | 수강생 |
| lecture_id | INT UNSIGNED | FK → lectures(id) | 강의 |
| watched_sec | INT UNSIGNED | NOT NULL, DEFAULT 0 | 시청 위치(초) |
| is_completed | TINYINT(1) | NOT NULL, DEFAULT 0 | 완료 여부 |
| last_watched | DATETIME | NOT NULL | 마지막 시청 시각 |

**UNIQUE KEY:** `(user_id, lecture_id)` — 수강생당 강의별 1개 레코드 (upsert)

## 3. 설계 결정 사항

### lecture_count 실시간 계산
강사가 커리큘럼을 추가/삭제할 때마다 courses 테이블의 lecture_count를 업데이트하면 정합성 문제가 생긴다. 대신 강의 목록/상세 조회 시 `COUNT(DISTINCT l.id)`로 실시간 계산한다. 강의 수가 자동으로 반영된다.

### progress upsert
같은 수강생이 같은 강의를 여러 번 시청할 때 레코드가 중복 생성되지 않도록 `(user_id, lecture_id)` UNIQUE KEY + `ON DUPLICATE KEY UPDATE`로 처리한다.

### wishlists 복합 PK
`(user_id, course_id)`를 복합 PK로 사용해 중복 찜을 DB 레벨에서 방지한다.

### 강사 가입 시 instructors 자동 등록
users.role = 'instructor'로 가입하면 authController에서 instructors 테이블에도 자동으로 레코드를 생성한다. 강의 등록 시 instructor_id를 users.name으로 조회해 연결한다.
